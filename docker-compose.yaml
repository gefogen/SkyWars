version: "3.3"

services:

  web:
    image: gefogen/sky-course-5
    build: .
    ports:
      - 80:5000

#  nginx:
#    image: nginx:1.19.3
#    ports:
#      - 80:80
#    volumes:
#      - ./nginx.conf:/etc/nginx/conf.d/default.conf
#    restart: always
